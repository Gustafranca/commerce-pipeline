apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Production: pin immutable digests or semver tags (update via release / CI).
images:
  - name: gfalmeida2/pipeline-commerce-backend
    newTag: latest
  - name: gfalmeida2/pipeline-commerce-frontend
    newTag: latest
